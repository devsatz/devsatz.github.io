export const metadata = {
  title: 'Privacy Policy',
  description:
    'Read the Zodiac Mind privacy policy to understand how horoscope and compatibility data is handled in the app.',
  alternates: {
    canonical: '/privacy-policy',
  },
};

export default function PrivacyPolicyLayout({ children }) {
  return children;
}
